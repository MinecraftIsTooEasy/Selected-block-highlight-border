# Selected block highlight border

## Modification of Block Selection Box and Entity Outline Colors
## Allows you to customize the color and transparency of the block selection highlight (the wireframe that appears when looking at a block) and the entity outline (the glowing border around entities, e.g., when using a spectral arrow or the glowing effect).
## Supports full RGB color customization and alpha (transparency) for both elements independently.